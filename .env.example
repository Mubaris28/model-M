# MongoDB Atlas - replace with your own URI if different
MONGODB_URI=mongodb+srv://mubarismuhammed33_db_user:QIwF75O9HZWeQdVK@cluster0.hxm5vc8.mongodb.net/model_management?retryWrites=true&w=majority&appName=Cluster0
JWT_SECRET=model-management-jwt-secret-change-in-production-xyz
ADMIN_EMAILS=admin@modelmanagement.mu
PORT=3001

# ─── FRONTEND (Next.js / Vercel) ───────────────────────────────────────────────
# Required in production! St this in your Vercel project → Settings → Environment Variables.
# Value: your Render backend root URL, e.g. https://model-m.onrender.com
# (no trailing slash, no /api suffix)
# Leave unset for local dev — Next.js rewrites /api/* to localhost:3001 automatically.
NEXT_PUBLIC_API_URL=https://model-m.onrender.com
